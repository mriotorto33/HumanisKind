feat: C2PA v2.3 live stream compatibility and dashboard integration

- **Core VSI Engine Updates (`src/vsi.ts`)**:
  - Upgraded Merkle chaining to be fully C2PA v2.3 compliant for live stream fMP4/HLS segments.
  - Implemented real-time cryptographic signature generation in `StreamSigner` using `crypto.createSign`.
  - Injected locally generated `signature` (base64url) and `certificate_pem` (P-256 public key) into the ISO BMFF `emsg` assertion payload.
  - Structural compatibility with Adobe C2PA validators achieved (untrusted cert warnings expected but payload parses successfully).
- **Next.js Boilerplate Updates**:
  - Saved auto-generated untrusted local certificates (`hik-signing.key.pem`, `hik-signing.pub.pem`) to the workspace.
  - Minor updates to `app/page.tsx` and `app/api/stream/route.ts` reflecting new live stream capabilities for dashboard visualization.
  - Updated API routes and package configuration for the boilerplate.
- **Miscellaneous**:
  - Restored proper configurations for Polygon networks in `.env` and `hardhat.config.ts`.
